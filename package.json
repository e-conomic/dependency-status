{
  "name": "dependency-status",
  "version": "0.2.7",
  "description": "Check whether your public npm dependencies (or private github dependencies) are up-to-date",
  "repository": "git://github.com/e-conomic/dependency-status.git",
  "dependencies": {
    "after-all": "^0.1.1",
    "semver": "^4.3.2",
    "request": "^2.37.0"
  }
}
